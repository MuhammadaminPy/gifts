# 🎰 Telegram Casino Bot - Инструкция по установке и запуску

## 📋 Содержание
1. [Требования](#требования)
2. [Структура проекта](#структура-проекта)
3. [Настройка Frontend (GitHub Pages)](#настройка-frontend)
4. [Настройка Backend](#настройка-backend)
5. [Настройка Telegram Bot](#настройка-telegram-bot)
6. [Запуск проекта](#запуск-проекта)
7. [API документация](#api-документация)

---

## 📦 Требования

### Необходимое ПО:
- Python 3.9+
- Git
- GitHub аккаунт
- Telegram Bot Token (получить у @BotFather)

### Python библиотеки:
```
aiogram==3.4.1
aiohttp==3.9.3
aiosqlite==0.19.0
python-dotenv==1.0.0
```

---

## 🗂️ Структура проекта

```
telegram-casino-bot/
├── backend/
│   └── bot.py                 # Основной файл Telegram бота
├── database/
│   ├── db_manager.py          # Менеджер базы данных
│   └── casino.db              # SQLite база (создается автоматически)
├── frontend/
│   └── index.html             # Главная страница TMA
├── static/
│   ├── css/
│   │   └── style.css          # Стили приложения
│   └── js/
│       ├── config.js          # Конфигурация
│       ├── api.js             # API клиент
│       ├── ui.js              # UI утилиты
│       ├── app.js             # Главное приложение
│       └── games/
│           ├── gift-upgrade.js # Игра Gift Upgrade
│           ├── rolls.js        # Игра Rolls
│           └── mutants.js      # Игра Mutants
├── requirements.txt           # Python зависимости
├── .env.example              # Пример переменных окружения
└── README.md                 # Эта инструкция
```

---

## 🌐 Настройка Frontend (GitHub Pages)

### Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на github.com
2. Создайте новый репозиторий с именем `telegram-casino-bot`
3. Сделайте его публичным

### Шаг 2: Загрузите файлы Frontend

Загрузите следующие файлы в корень репозитория:
```
- index.html (из папки frontend)
- static/ (вся папка со всем содержимым)
```

### Шаг 3: Включите GitHub Pages

1. Зайдите в Settings вашего репозитория
2. Перейдите в раздел "Pages"
3. В Source выберите "main branch"
4. Сохраните настройки

### Шаг 4: Обновите конфигурацию

Откройте `static/js/config.js` и обновите:

```javascript
const CONFIG = {
    // Замените на URL вашего бэкенда (см. настройку Backend)
    API_URL: 'https://your-backend-url.com',
    
    // Замените на username вашего бота
    BOT_USERNAME: 'YourBotUsername',
    
    // ... остальные настройки
};
```

Ваш Frontend теперь доступен по адресу:
`https://yourusername.github.io/telegram-casino-bot/`

---

## 🔧 Настройка Backend

### Вариант 1: Локальный запуск (для тестирования)

1. **Установите Python зависимости:**
```bash
pip install -r requirements.txt
```

2. **Создайте .env файл:**
```bash
cp .env.example .env
```

3. **Отредактируйте .env:**
```env
BOT_TOKEN=your_bot_token_from_botfather
ADMIN_ID=your_telegram_user_id
WEBAPP_URL=https://yourusername.github.io/telegram-casino-bot/
```

4. **Запустите бота:**
```bash
python backend/bot.py
```

### Вариант 2: Деплой на сервер (Heroku, VPS, и т.д.)

#### Heroku:

1. **Установите Heroku CLI**

2. **Создайте Procfile:**
```
web: python backend/bot.py
```

3. **Деплой:**
```bash
heroku create your-casino-bot
heroku config:set BOT_TOKEN=your_token
heroku config:set ADMIN_ID=your_id
heroku config:set WEBAPP_URL=https://yourusername.github.io/telegram-casino-bot/
git push heroku main
```

#### VPS (Ubuntu):

1. **Установите Python:**
```bash
sudo apt update
sudo apt install python3 python3-pip
```

2. **Клонируйте репозиторий:**
```bash
git clone https://github.com/yourusername/telegram-casino-bot.git
cd telegram-casino-bot
```

3. **Установите зависимости:**
```bash
pip3 install -r requirements.txt
```

4. **Настройте systemd service:**
```bash
sudo nano /etc/systemd/system/casino-bot.service
```

Добавьте:
```ini
[Unit]
Description=Telegram Casino Bot
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/path/to/telegram-casino-bot
ExecStart=/usr/bin/python3 backend/bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

5. **Запустите сервис:**
```bash
sudo systemctl enable casino-bot
sudo systemctl start casino-bot
```

---

## 🤖 Настройка Telegram Bot

### Шаг 1: Создайте бота

1. Найдите @BotFather в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Сохраните токен

### Шаг 2: Настройте Web App

Отправьте @BotFather команды:

```
/setmenubutton
[выберите вашего бота]
[введите название кнопки, например "Play Casino"]
[вставьте ссылку на ваш GitHub Pages]
```

### Шаг 3: Настройте команды бота

```
/setcommands
start - Запустить бота
admin - Админ панель (только для админа)
```

---

## 🚀 Запуск проекта

### 1. Frontend готов
✅ Загружен на GitHub Pages
✅ Доступен по адресу `https://yourusername.github.io/telegram-casino-bot/`

### 2. Backend запущен
✅ Локально или на сервере
✅ База данных инициализирована

### 3. Bot настроен
✅ Токен добавлен в `.env`
✅ Web App кнопка настроена

### 4. Тестирование

1. Откройте вашего бота в Telegram
2. Нажмите `/start`
3. Нажмите кнопку меню "Play Casino"
4. Должно открыться приложение!

---

## 📡 API документация

### User Endpoints

#### GET `/api/user/data`
Получить данные пользователя
```json
{
  "user": {
    "user_id": 123456,
    "username": "username",
    "balance": 10.5,
    "ref_balance": 2.3
  }
}
```

#### GET `/api/user/balance`
Получить баланс
```json
{
  "balance": 10.5,
  "ref_balance": 2.3
}
```

#### POST `/api/user/activity`
Обновить активность пользователя

### Game Endpoints

#### POST `/api/games/gift-upgrade/play`
Сыграть в Gift Upgrade
```json
{
  "bet_amount": 1.0,
  "multiplier": 2
}
```

Response:
```json
{
  "result": "win",
  "bet_amount": 1.0,
  "win_amount": 2.0,
  "balance": 11.5
}
```

#### POST `/api/games/rolls/bet`
Сделать ставку в Rolls
```json
{
  "color": "red",
  "amount": 1.0
}
```

#### POST `/api/games/mutants/open-case`
Открыть кейс
```json
{
  "case_name": "REGULAR_1"
}
```

Response:
```json
{
  "reward_name": "Jolly Chimp",
  "reward_value": 15,
  "balance": 8.5
}
```

### Deposit/Withdrawal Endpoints

#### POST `/api/deposit/create`
Создать депозит
```json
{
  "amount": 10,
  "method": "ton"
}
```

#### POST `/api/withdrawal/create`
Создать запрос на вывод
```json
{
  "amount": 10,
  "wallet": "EQD..."
}
```

### Leaderboard & Referrals

#### GET `/api/leaderboard?limit=35`
Получить топ игроков

#### GET `/api/referral/data`
Получить реферальные данные

#### POST `/api/referral/transfer`
Перевести реферальный баланс на основной

---

## 🎮 Игры

### 1. Gift Upgrade
- Выберите ставку (мин. 0.01 TON)
- Выберите множитель (1.3x - 20x)
- Вращайте рулетку
- Процент выигрыша зависит от множителя

### 2. Rolls
- Игра в реальном времени (раунд каждые 10 сек)
- Ставьте на красное/синее (2x) или зеленое (10x)
- 100 фишек: 49 красных, 49 синих, 2 зеленых

### 3. Mutants (Cases)
- Доступна после пополнения от 5 TON
- 4 типа кейсов
- NFT подарки и TON награды
- Продажа предметов из инвентаря

---

## 🔐 Безопасность

### Важно:
1. **Никогда не коммитьте `.env` файл в Git!**
2. Добавьте `.env` в `.gitignore`
3. Храните `BOT_TOKEN` в секрете
4. Регулярно делайте бэкап базы данных

### .gitignore:
```
.env
*.db
__pycache__/
*.pyc
node_modules/
```

---

## 🐛 Решение проблем

### Проблема: Бот не отвечает
- Проверьте, запущен ли backend
- Проверьте правильность BOT_TOKEN
- Проверьте логи: `tail -f bot.log`

### Проблема: Web App не открывается
- Проверьте URL в конфигурации бота
- Убедитесь, что GitHub Pages активирован
- Проверьте консоль браузера на ошибки

### Проблема: База данных не создается
- Проверьте права доступа к папке
- Запустите `python backend/bot.py` вручную
- Проверьте, установлен ли aiosqlite

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи бота
2. Откройте DevTools в браузере (F12)
3. Проверьте Network tab на ошибки API

---

## 📝 Лицензия

Этот проект создан в образовательных целях.

---

**Готово! Ваш Casino Bot должен работать! 🎉**

Не забудьте:
- ✅ Обновить BOT_TOKEN
- ✅ Обновить WEBAPP_URL
- ✅ Обновить CONFIG в config.js
- ✅ Настроить админ ID
